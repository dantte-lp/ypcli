Share the following secret securely and give me back only the one-time URL
(never echo the secret itself). Use the ypcli MCP `send_secret` tool (or
`ypcli send --json` if the tool is unavailable), with `one_time` on and the
shortest reasonable expiration.

Secret: $1
